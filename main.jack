
/** Initializes a new Square Dance game and starts running it. */
class Main {
    function void main() {
        var snakeGame game;
        let game = snakeGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
